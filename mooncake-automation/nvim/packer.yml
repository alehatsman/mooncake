---
- name: Make sure packer is installed if it is not already there
  shell: |
    [ ! -d "~/.local/share/nvim/site/pack/packer/start/packer.nvim" ] && git clone --depth 1 https://github.com/wbthomason/packer.nvim ~/.local/share/nvim/site/pack/packer/start/packer.nvim; exit 0;
