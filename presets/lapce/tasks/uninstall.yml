- name: Uninstall lapce
  package:
    name: lapce
    state: absent
  failed_when: false

- name: Assert lapce is removed
  assert:
    command:
      cmd: command -v lapce
      exit_code: 1

- name: Display confirmation
  print: "lapce uninstalled"
