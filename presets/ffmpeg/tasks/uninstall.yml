- name: Uninstall ffmpeg
  package:
    name: ffmpeg
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v ffmpeg
      exit_code: 1

- name: Display confirmation
  print: "ffmpeg uninstalled"
