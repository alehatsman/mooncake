- name: Uninstall direnv
  pkg:
    name: direnv
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v direnv
      exit_code: 1

- name: Display confirmation
  log: "direnv uninstalled"
