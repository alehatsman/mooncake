- name: Uninstall chamber
  package:
    name: chamber
    state: absent
  failed_when: false

- name: Display confirmation
  print: "chamber uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v chamber
      exit_code: 1
