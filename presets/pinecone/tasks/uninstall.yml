- name: Uninstall pinecone client
  shell: pip3 uninstall -y pinecone-client
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: python3 -c "import pinecone"
      exit_code: 1

- name: Display confirmation
  print: "pinecone client uninstalled"
