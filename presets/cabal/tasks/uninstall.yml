- name: Uninstall cabal
  pkg:
    name: cabal
    state: absent
  failed_when: false

- name: Display confirmation
  log: "cabal uninstalled"
- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v cabal
      exit_code: 1
