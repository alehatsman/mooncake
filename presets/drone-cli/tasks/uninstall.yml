- name: Uninstall drone-cli
  package:
    name: drone-cli
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v drone
      exit_code: 1

- name: Display confirmation
  print: "drone-cli uninstalled"
