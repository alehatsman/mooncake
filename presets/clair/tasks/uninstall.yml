- name: Uninstall clair
  package:
    name: clair
    state: absent
  failed_when: false

- name: Display confirmation
  print: "clair uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v clair
      exit_code: 1
