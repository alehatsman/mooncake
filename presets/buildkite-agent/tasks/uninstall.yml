- name: Uninstall buildkite-agent
  package:
    name: buildkite-agent
    state: absent
  failed_when: false

- name: Display confirmation
  print: "buildkite-agent uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v buildkite-agent
      exit_code: 1
