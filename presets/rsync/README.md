# rsync

File transfer / backup tool

## Quick Start

```yaml
- preset: rsync
```

## Usage

```bash
# Basic usage
rsync --help

# Common operations
rsync --version
```

## Agent Use

Automation-friendly CLI tool with:
- Exit codes for error handling  
- JSON/YAML output support (where applicable)
- Scriptable interface
- Idempotent operations

## Resources

Search: "rsync documentation" or "rsync github"
