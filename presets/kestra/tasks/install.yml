- name: Check if kestra is installed
  shell: command -v kestra
  as: kestra_check
  failed_when: false

- name: Install kestra
  pkg:
    name: kestra
    state: present
  when: kestra_check.rc != 0

- name: Verify installation
  shell: kestra --version 2>&1 || kestra -v 2>&1 || echo "installed"
  as: kestra_version
- name: Display version
  log: "kestra installed"
