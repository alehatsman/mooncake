- name: Uninstall flyte
  package:
    name: flyte
    state: absent
  failed_when: false

- name: Assert flytectl is removed
  assert:
    command:
      cmd: command -v flytectl
      exit_code: 1

- name: Display confirmation
  print: "flyte uninstalled"
