# nats-cli

Message queue / streaming

## Quick Start

```yaml
- preset: nats-cli
```

## Usage

```bash
# Basic usage
nats-cli --help

# Common operations
nats-cli --version
```

## Agent Use

Automation-friendly CLI tool with:
- Exit codes for error handling  
- JSON/YAML output support (where applicable)
- Scriptable interface
- Idempotent operations

## Resources

Search: "nats-cli documentation" or "nats-cli github"
