- name: Uninstall doctl
  package:
    name: doctl
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v doctl
      exit_code: 1

- name: Display confirmation
  print: "doctl uninstalled"
