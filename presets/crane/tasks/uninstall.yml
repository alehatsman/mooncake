- name: Uninstall crane
  package:
    name: crane
    state: absent
  failed_when: false

- name: Display confirmation
  print: "crane uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v crane
      exit_code: 1
