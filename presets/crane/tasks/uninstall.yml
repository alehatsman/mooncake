- name: Uninstall crane
  pkg:
    name: crane
    state: absent
  failed_when: false

- name: Display confirmation
  log: "crane uninstalled"
- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v crane
      exit_code: 1
