- name: Uninstall uptrace (macOS)
  file.write:
    path: /usr/local/bin/uptrace
    state: absent
  as_user: root
  when: brew_available
  failed_when: false

- name: Uninstall uptrace (Linux)
  file.write:
    path: /usr/local/bin/uptrace
    state: absent
  as_user: root
  when: linux
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v uptrace
      exit_code: 1

- name: Display confirmation
  log: "uptrace uninstalled"
