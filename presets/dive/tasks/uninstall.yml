- name: Uninstall dive
  package:
    name: dive
    state: absent
  failed_when: false

- name: Display confirmation
  print: "dive uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v dive
      exit_code: 1
