- name: Uninstall dive
  pkg:
    name: dive
    state: absent
  failed_when: false

- name: Display confirmation
  log: "dive uninstalled"
- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v dive
      exit_code: 1
