- name: Uninstall cosign
  pkg:
    name: cosign
    state: absent
  failed_when: false

- name: Display confirmation
  log: "cosign uninstalled"
- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v cosign
      exit_code: 1
