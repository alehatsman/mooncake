name: scaleway-cli
description: Manage Scaleway cloud resources from the terminal
version: "1.0.0"
parameters:
  state:
    type: string
    required: false
    default: present
    enum: [present, absent]
steps:
  - name: Install scaleway-cli
    import: tasks/install.yml
    when: parameters.state == "present"
  - name: Uninstall scaleway-cli
    import: tasks/uninstall.yml
    when: parameters.state == "absent"
