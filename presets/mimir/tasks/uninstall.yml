- name: Uninstall mimir
  pkg:
    name: mimir
    state: absent
  failed_when: false

- name: Display confirmation
  log: "mimir uninstalled"
- name: Assert tool is removed
  assert:
    command:
      cmd: command -v mimir
      exit_code: 1
