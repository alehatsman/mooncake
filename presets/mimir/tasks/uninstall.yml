- name: Uninstall mimir
  package:
    name: mimir
    state: absent
  failed_when: false

- name: Display confirmation
  print: "mimir uninstalled"

- name: Assert tool is removed
  assert:
    command:
      cmd: command -v mimir
      exit_code: 1
