- name: Check if mimir is installed
  shell: command -v mimir
  as: mimir_check
  failed_when: false

- name: Install mimir
  pkg:
    name: mimir
    state: present
  when: mimir_check.rc != 0

- name: Verify installation
  shell: mimir --version 2>&1 || mimir -v 2>&1 || echo "installed"
  as: mimir_version
- name: Display version
  log: "mimir installed"
- name: Verify installation
  assert:
    command:
      cmd: command -v mimir
      exit_code: 0
