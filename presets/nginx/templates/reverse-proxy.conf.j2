server {
    listen {{ port | default('80') }};
    listen [::]:{{ port | default('80') }};
    server_name {{ server_name | default('example.com') }};

    # Logging
    access_log /var/log/nginx/{{ server_name | default('proxy') }}_access.log;
    error_log /var/log/nginx/{{ server_name | default('proxy') }}_error.log;

    location / {
        proxy_pass {{ backend_url | default('http://localhost:3000') }};
        proxy_http_version 1.1;

        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
