- name: Uninstall prefect
  shell: pip3 uninstall -y prefect
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v prefect
      exit_code: 1

- name: Display confirmation
  print: "prefect uninstalled"
