- name: Check if zenith is installed
  shell: command -v zenith
  register: zenith_check
  failed_when: false

- name: Install zenith (macOS)
  package:
    name: zenith
    state: present
  when: os == "darwin" and zenith_check.rc != 0

- name: Install zenith (Linux)
  shell: cargo install zenith
  when: os == "linux" and zenith_check.rc != 0

- name: Verify installation with assert
  assert:
    command:
      cmd: command -v zenith
      exit_code: 0

- name: Display version
  print: "zenith installed"
