- name: Uninstall fx
  package:
    name: fx
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v fx
      exit_code: 1

- name: Display confirmation
  print: "fx uninstalled"
