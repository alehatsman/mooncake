- name: Check if volta is installed
  shell: command -v volta
  as: volta_check
  failed_when: false

- name: Install volta (macOS)
  pkg:
    name: volta
    state: present
  when: brew_available and volta_check.rc != 0

- name: Install volta (Linux)
  shell: curl https://get.volta.sh | bash
  when: linux and volta_check.rc != 0

- name: Verify installation with assert
  assert:
    command:
      cmd: command -v volta
      exit_code: 0

- name: Display confirmation
  log: "volta installed"
