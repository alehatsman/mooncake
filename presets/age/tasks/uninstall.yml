- name: Uninstall age
  package:
    name: age
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v age
      exit_code: 1

- name: Display confirmation
  print: "age uninstalled"
