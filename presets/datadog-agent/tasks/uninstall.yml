- name: Uninstall datadog-agent
  package:
    name: datadog-agent
    state: absent
  failed_when: false

- name: Display confirmation
  print: "datadog-agent uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v datadog-agent
      exit_code: 1
