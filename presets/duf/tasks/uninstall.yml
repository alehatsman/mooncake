- name: Uninstall duf
  package:
    name: duf
    state: absent
  failed_when: false

- name: Display confirmation
  print: "duf uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v duf
      exit_code: 1
