- name: Uninstall entr
  package:
    name: entr
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v entr
      exit_code: 1

- name: Display confirmation
  print: "entr uninstalled"
