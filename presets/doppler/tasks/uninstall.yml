- name: Uninstall doppler
  package:
    name: doppler
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v doppler
      exit_code: 1

- name: Display confirmation
  print: "doppler uninstalled"
