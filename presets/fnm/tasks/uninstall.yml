- name: Uninstall fnm
  package:
    name: fnm
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v fnm
      exit_code: 1

- name: Display confirmation
  print: "fnm uninstalled"
