- name: Check if zoxide is installed
  shell: command -v zoxide
  register: zoxide_check
  failed_when: false

- name: Install zoxide (macOS)
  package:
    name: zoxide
    state: present
  when: os == "darwin" and zoxide_check.rc != 0

- name: Install zoxide (Linux)
  shell: cargo install zoxide
  when: os == "linux" and zoxide_check.rc != 0

- name: Verify installation with assert
  assert:
    command:
      cmd: command -v zoxide
      exit_code: 0

- name: Display version
  print: "zoxide installed"
