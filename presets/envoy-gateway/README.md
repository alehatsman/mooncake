# envoy-gateway

API gateway / load balancer

## Quick Start

```yaml
- preset: envoy-gateway
```

## Usage

```bash
# Basic usage
envoy-gateway --help

# Common operations
envoy-gateway --version
```

## Agent Use

Automation-friendly CLI tool with:
- Exit codes for error handling  
- JSON/YAML output support (where applicable)
- Scriptable interface
- Idempotent operations

## Resources

Search: "envoy-gateway documentation" or "envoy-gateway github"
