# Hyprland Uninstall Tasks

- name: Remove Hyprland ecosystem packages
  shell: |
    pacman -Rns --noconfirm \
      hyprland waybar wofi dunst \
      hyprpaper hyprlock hypridle \
      xdg-desktop-portal-hyprland \
      grim slurp 2>/dev/null || true
  as_user: root
  when: pacman_available
