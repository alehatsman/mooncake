- name: Uninstall editorconfig
  pkg:
    name: editorconfig
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v editorconfig
      exit_code: 1

- name: Display confirmation
  log: "editorconfig uninstalled"
