- name: Uninstall milvus
  package:
    name: milvus
    state: absent
  failed_when: false

- name: Display confirmation
  print: "milvus uninstalled"

- name: Assert tool is removed
  assert:
    command:
      cmd: command -v milvus
      exit_code: 1
