- name: Uninstall checkov
  package:
    name: checkov
    state: absent
  failed_when: false

- name: Display confirmation
  print: "checkov uninstalled"

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v checkov
      exit_code: 1
