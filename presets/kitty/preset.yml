name: kitty
description: Fast GPU-based terminal emulator
version: "1.0.0"
parameters:
  state:
    type: string
    required: false
    default: present
    enum: [present, absent]
steps:
  - name: Install kitty
    import: tasks/install.yml
    when: parameters.state == "present"
  - name: Uninstall kitty
    import: tasks/uninstall.yml
    when: parameters.state == "absent"
