# Finish-Line Priorities — 2026-05-15

> Ordered work list to reach logical conclusion on all open ideas before
> switching to a software-quality iteration. Generated by auditing
> `docs-working/` against the codebase and git log.

---

## What just moved to done/

The following specs shipped since the last docs-working cleanup and have
been moved to `specs/done/`:

| Spec | Topic |
|---|---|
| 30 | `transaction:` blocks — LIFO rollback (PR A `7c2c00e` + PR B `15cdc79`) |
| 35 | `mooncake plan --diff` content diffs |
| 44 | SSH bootstrap transport (native ssh driver + sftp) |
| 46 | `fleet status`, `fleet logs`, `fleet facts` |
| 47 | `mooncake fleet bootstrap` + `fleet pair` UX |
| 48 | Per-host overlays + `--peer-filter` / `--step-filter` |
| 50 | Extended filter keys (`name=`, `os=`, `role=`) |
| 51 | Local-apply overlay parity (`vars/by-host/<hostname>.yml`) |

Personal Fleet: **13 of 14 PRs shipped** (Phase A + B + C 2/3). Only
`fleet init` interactive flow (PR 13) remains.

---

## Open work — ordered by priority

### P0 — Bug (fix before everything)

**`file.write` symlink + `force: true` aborts `mooncake plan`** when the
target path is a non-symlink directory. `mooncake apply` works correctly;
only plan-mode prediction is broken. Real user hit in dotfiles bootstrap.

- Fix sketched in `bug/bug-symlink-force-plan-inspect.md`.
- 3 files, ~30 LOC production change + 5 new test cases.
- No dependencies on any spec.

---

### P1 — Spec-22 phases 7–8 (MCP wiring + docs)

**Why first**: the four-method ABI is fully declared across all 15 priority
handlers (phases 1–6 ✅). Without phase 7, agents and UIs must parse prose
to learn what a plan would change and how much it would cost. The MCP server
currently exposes `check_plan` / `run_plan` but not the structural `Diff` or
`Cost` outputs that make those tools useful for branching.

**Phase 7 scope** (~200–300 LOC):
- Wire `Diff()` output into `check_plan` / `run_plan` MCP tool responses.
- Wire `Cost()` aggregate into plan-JSON and MCP responses.
- Surface `Permissions()` in the MCP `run_plan` tool so the agent can see
  what capabilities a plan requires before submitting.

**Phase 8 scope**: update `docs/` with the new ABI surface (handler author
guide + agent consumer guide). Unblocks lighthouse-user demos.

---

### P2 — Spec-23 §2: `try` / `catch` / `finally`

**Why second**: §1 (`on_change`) ✅ and §3 (`!secret`) ✅ are done. §2 is
the last piece of the framework-primitives trio. The spec-30 transactions
landing resolved the semantic-overlap concern — `try/catch` is user-authored
rollback; `transaction:` is ABI-automated rollback. They're complementary.

Effort: M (~400–600 LOC). Parser + executor changes. No handler interface
changes needed.

---

### P3 — Spec-37 + Spec-38: step output capture + `read.json` / `read.yaml`

**Why third**: closes the single most common "workaround" pattern — shelling
out to `jq` / `yq` to read a value from a file. In sandboxed agent mode
(Stream 2 goal), `shell:` disappears and these workarounds break completely.

- Spec-37 (XS, 1 day): define collision policy and plan-mode write policy
  for `as:` captures. No new actions; pure executor policy.
- Spec-38 (S, 2–3 days): add `read.json` and `read.yaml` actions. Depends
  on spec-37's `CaptureInPlan` mechanism.

Both are self-contained. No handler ABI changes.

---

### P4 — Spec-24 P6 + Spec-25 P5: ABI hooks for `pkg.*` and `text.*`

**Why fourth**: these were blocked on spec-22 phase 3 (Permissions). That
shipped. The final ABI-hook phase for the two most-used action families is
now unblocked. Together they close the "Stream-1 specs' final phases" line
from the success criteria.

- Spec-24 P6: wire `Permissions()` / `Diff()` / `Reverse()` / `Cost()` into
  all `pkg.*` handlers using the existing spec-22 patterns.
- Spec-25 P5: same for `text.*` handlers.

Effort: S each (~1 day per family). Pure pattern-application; handler
interface is already defined.

---

### P5 — Spec-26 + Spec-27: ABI hooks for `git.*` and `os.*` identity

**Why fifth**: same pattern as P4. Phases 1–4 of spec-26 (git.clone,
git.checkout, git.config) and phases 1–3 of spec-27 (os.user, os.group,
os.ssh_key) shipped. The ABI-hook phases remain.

Lower priority than P4 because git + identity actions are less frequently
agent-touched than pkg + text.

---

### P6 — Fleet init (spec-45 PR13)

**Why sixth**: last remaining fleet item. Pure UX polish — `mooncake fleet
init` merges mDNS (✅ `70476f6`) + SSH config + static peers into an
interactive prompt loop. No capability gap behind it; operators can
hand-edit `peers.toml` today.

Effort: S (~350 LOC). Pull forward if a user reports friction.

---

### P7 — Fleet QoL bundle (specs 52–55)

**Why seventh**: small, daily-use additions drafted from real fleet operation.
Build on existing agentd infrastructure; no new daemon endpoints for most.

| Spec | Command | Effort | Notes |
|---|---|---|---|
| 54 | `fleet ps` | XS (~100 LOC) | Fan-out `GET /v1/runs?status=running`, tabwriter |
| 52 | `fleet exec '<cmd>'` | S (~200 LOC) | Synthesize in-memory one-step plan, submit through existing pipeline |
| 53 | `fleet watch` | S (~150 LOC) | Persistent SSE multi-peer subscription |
| 55 | `fleet doctor` | S (~300 LOC) | Fan-out kernel doctor, aggregate pass/fail table |

Ship as a bundle or individually; `fleet ps` is the cheapest and most useful
entry point.

---

### P8 — Spec-32: collapse step action dispatch

**Why eighth**: structural refactor with real bug payoff (ServiceAction deep
copy is shallow; 21 of 28 actions are silently skipped in
`renderActionTemplates`). Not user-visible today but adds an invisible
correctness tax on every new action added.

Effort: M (1–2 weeks). Core config + planner files. High risk of subtle
regressions — needs careful test coverage.

---

### P9 — Spec-36: Windows native improvements (phases 1–3)

**Why ninth**: agentd on Windows shipped (spec-49 ✅). The remaining
spec-36 work is mooncake.exe itself: PowerShell shell routing, `winget`
package manager, cross-platform test audit (28 skipped tests). Real value
for Windows-first provisioning, but lower urgency than agent-safety items.

Phases:
1. Shell action OS routing (split `exec_unix.go` / `exec_windows.go`, default interpreter → `powershell`)
2. `winget` in package action
3. Cross-platform test audit (28 skipped tests → fix or document each)

---

### Deferred (explicit not-now)

| Item | Why deferred | Revisit when |
|---|---|---|
| spec-28 non-ufw drivers (nftables/firewalld/pf) | Driver sprawl; ufw covers 80% | Community PR or paid request |
| spec-31 tier-2 plugin model | Premature without lighthouse users | 2+ external contributors blocked by a fork |
| Enterprise hub / cluster specs | No paying users | Platform team adopts + asks |
| Rename to "Edict" | Zero leverage before users | After 100+ stars / 10+ active users |
| Deterministic replay command | Implicit in run audit; no user has asked | Agent project needs replay for debugging |
| Policy DSL (`deny:` patterns) | Hooks exist via Permissions; no user-driven case | User asks "how do I prevent the agent from touching /etc/passwd" |

---

## Summary table

| Priority | Item | Effort | Stream |
|---|---|---|---|
| **P0** | Bug: symlink force plan-mode crash | XS (30 LOC) | Bug |
| **P1** | Spec-22 phase 7–8: MCP wiring + docs | S–M | Stream 2 |
| **P2** | Spec-23 §2: try/catch/finally | M | Stream 2 |
| **P3** | Spec-37 + 38: output capture + read.json/yaml | XS + S | Stream 1 |
| **P4** | Spec-24 P6 + 25 P5: pkg/text ABI hooks | S each | Stream 1 |
| **P5** | Spec-26 + 27: git/identity ABI hooks | S each | Stream 1 |
| **P6** | Spec-45 PR13: `fleet init` interactive | S | Stream 3 |
| **P7** | Specs 52–55: fleet exec/ps/watch/doctor | XS–S each | Stream 3 |
| **P8** | Spec-32: step action dispatch collapse | M | Stream 1 |
| **P9** | Spec-36: Windows phases 1–3 | M | Stream 1 |
