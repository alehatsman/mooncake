# Generated by `mooncake init --template empty`
# Edit freely. Preview changes with `mooncake plan`. Apply with `mooncake apply`.
#
# Add steps below. Run `mooncake actions list` for the typed action surface,
# or `mooncake mod add <host>/<owner>/<repo>@<tag>` to pull in a shared module.
#
# Variables in mooncake.vars.yml are loaded automatically — no flag needed.

- name: Ensure ~/.mooncake/scratch exists
  file.write:
    path: "{{ home }}/.mooncake/scratch"
    state: directory
