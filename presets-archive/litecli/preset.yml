name: litecli
description: SQLite CLI with autocomplete
version: "1.0.0"
parameters:
  state:
    type: string
    required: false
    default: present
    enum: [present, absent]
steps:
  - name: Install litecli
    import: tasks/install.yml
    when: parameters.state == "present"
  - name: Uninstall litecli
    import: tasks/uninstall.yml
    when: parameters.state == "absent"
