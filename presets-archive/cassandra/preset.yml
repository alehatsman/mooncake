name: cassandra
description: Apache Cassandra CLI
version: "1.0.0"
parameters:
  state:
    type: string
    required: false
    default: present
    enum: [present, absent]
steps:
  - name: Install cassandra
    import: tasks/install.yml
    when: parameters.state == "present"
  - name: Uninstall cassandra
    import: tasks/uninstall.yml
    when: parameters.state == "absent"
