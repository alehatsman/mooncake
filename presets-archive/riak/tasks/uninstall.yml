- name: Uninstall riak
  pkg:
    name: riak
    state: absent
  as_user: root
  failed_when: false

- name: Assert riak is removed
  assert:
    command:
      cmd: command -v riak
      exit_code: 1

- name: Display confirmation
  log: "riak uninstalled"
