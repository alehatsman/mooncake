- name: Check if sccache is installed
  shell: command -v sccache
  as: sccache_check
  failed_when: false

- name: Install sccache (macOS)
  pkg:
    name: sccache
    state: present
  when: brew_available and sccache_check.rc != 0

- name: Install sccache (Linux)
  shell: cargo install sccache
  when: linux and sccache_check.rc != 0

- name: Verify installation with assert
  assert:
    command:
      cmd: command -v sccache
      exit_code: 0

- name: Display confirmation
  log: "sccache installed"
