- name: Uninstall clojure
  pkg:
    name: clojure
    state: absent
  failed_when: false

- name: Display confirmation
  log: "clojure uninstalled"
- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v clj
      exit_code: 1
