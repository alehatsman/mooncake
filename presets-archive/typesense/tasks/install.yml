- name: Check if typesense is installed
  shell: command -v typesense
  as: typesense_check
  failed_when: false

- name: Install typesense
  pkg:
    name: typesense
    state: present
  when: typesense_check.rc != 0

- name: Verify installation
  shell: typesense --version 2>&1 || typesense -v 2>&1 || echo "installed"
  as: typesense_version
- name: Display version
  log: "typesense installed"
