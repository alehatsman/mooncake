- name: Uninstall artemis via package manager
  pkg:
    name: artemis
    state: absent
  failed_when: false

- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v artemis
      exit_code: 1

- name: Display confirmation
  log: "artemis uninstalled"
