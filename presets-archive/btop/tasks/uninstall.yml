- name: Uninstall btop
  pkg:
    name: btop
    state: absent
  failed_when: false

- name: Display confirmation
  log: "btop uninstalled"
- name: Verify uninstallation with assert
  assert:
    command:
      cmd: command -v btop
      exit_code: 1
