- name: Check if redis-cli is installed
  shell: command -v redis-cli
  as: redis_cli_check
  failed_when: false

- name: Install redis-cli (macOS)
  pkg:
    name: redis
    state: present
  when: brew_available and redis_cli_check.rc != 0

- name: Install redis-cli (Linux)
  pkg:
    name: redis-tools
    state: present
  as_user: root
  when: apt_available and redis_cli_check.rc != 0

- name: Verify installation with assert
  assert:
    command:
      cmd: command -v redis-cli
      exit_code: 0

- name: Display confirmation
  log: "redis-cli installed"
