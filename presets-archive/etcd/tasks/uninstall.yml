- name: Uninstall etcd
  pkg:
    name: etcd
    state: absent
  failed_when: false

- name: Assert etcd is removed
  assert:
    command:
      cmd: command -v etcd
      exit_code: 1

- name: Display confirmation
  log: "etcd uninstalled"
