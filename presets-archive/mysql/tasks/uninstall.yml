# MySQL Uninstallation Tasks

- name: Stop MySQL service (Linux)
  os.service:
    name: mysql
    state: stopped
  as_user: root
  failed_when: false
  when: linux

- name: Stop MySQL service (macOS)
  shell: brew services stop mysql
  failed_when: false
  when: brew_available

# macOS Uninstallation
- name: Uninstall MySQL via Homebrew (macOS)
  pkg:
    name: mysql
    state: absent
  when: brew_available

# Linux Uninstallation
- name: Uninstall MySQL (Debian/Ubuntu)
  pkg:
    name: mysql-server
    state: absent
  as_user: root
  when: apt_available

- name: Uninstall MySQL (RHEL/Fedora/CentOS)
  pkg:
    name: mysql-server
    state: absent
  as_user: root
  when: dnf_available or yum_available

- name: Uninstall MySQL (Arch)
  pkg:
    name: mysql
    state: absent
  as_user: root
  when: pacman_available

- name: Verify mysql is not installed
  assert:
    command:
      cmd: command -v mysql
      exit_code: 1
  failed_when: false

- name: Display uninstall confirmation
  log: "MySQL has been uninstalled (data directory may remain)"
