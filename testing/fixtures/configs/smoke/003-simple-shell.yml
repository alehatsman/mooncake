- name: Echo test
  shell: echo "Hello from mooncake"
  register: output

- name: Run command with pipe
  shell: echo "test" | tr '[:lower:]' '[:upper:]'
  register: upper

- name: Get current directory
  shell: pwd
  register: current_dir

- name: Display result
  shell: echo "Command executed successfully"
